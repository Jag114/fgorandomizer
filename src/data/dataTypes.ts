export interface ServantComponentProps {
  region: string,
  profile: string,
  setProfile: string
}

export interface ServantData{
  id: number | string,
  name: string,
  rarity: number,
  class: string
}
